{
  "name": "ai-wrist-dashboard",
  "version": "1.0.0",
  "private": true,
  "description": "Desktop/web dashboard for the AI Wrist Assistant local services.",
  "scripts": {
    "install:all": "npm --prefix backend install && npm --prefix frontend install",
    "dev": "powershell -NoProfile -ExecutionPolicy Bypass -File .\\start-dashboard.ps1",
    "stop": "powershell -NoProfile -ExecutionPolicy Bypass -File .\\stop-dashboard.ps1",
    "backend": "npm --prefix backend run start",
    "frontend": "npm --prefix frontend run dev",
    "build": "npm --prefix frontend run build"
  }
}
